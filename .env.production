# =============================================================================
# ASKJOEL.CLOUD — Joel Agent — PRODUÇÃO VPS
# =============================================================================

# === DJANGO ===
SECRET_KEY=GERAR_COM_python_-c_"from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
DEBUG=False
ALLOWED_HOSTS=askjoel.cloud,www.askjoel.cloud

# === DATABASE (PostgreSQL) ===
DB_ENGINE=postgresql
DB_NAME=askjoel_db
DB_USER=askjoel_user
DB_PASSWORD=GERAR_SENHA_FORTE_AQUI
DB_HOST=localhost
DB_PORT=5432

# === OPENAI ===
OPENAI_API_KEY=COPIAR_DO_.ENV_LOCAL
OPENAI_MODEL=gpt-4.1-mini

# === TAVILY ===
TAVILY_API_KEY=tvly-SUA-CHAVE-AQUI

# === JOEL AGENT ===
JOEL_TIMEOUT=120
JOEL_DEFAULT_LANGUAGE=pt-BR
JOEL_MAX_SEARCH_RESULTS=10
JOEL_SEARCH_DEPTH=advanced

# === GITHUB ===
GITHUB_REPO=interpreter
GITHUB_CLONE_URL=https://github.com/charliba/interpreter.git
GITHUB_BRANCH=main
