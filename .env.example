# =============================================================================
# INTERPRETADOR DE DOCUMENTOS — Joel Agent
# =============================================================================
# Copie este arquivo para .env e preencha com seus valores reais
# NUNCA commite o .env — apenas o .env.example vai para o Git
# =============================================================================

# === DJANGO ===
SECRET_KEY=change-me-to-a-random-string-50-chars
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
PORT=8004

# === DATABASE ===
# sqlite3 para dev local, postgresql para produção
DB_ENGINE=sqlite3
# DB_NAME=interpreter_db
# DB_USER=interpreter_user
# DB_PASSWORD=change-me
# DB_HOST=localhost
# DB_PORT=5432

# === OPENAI ===
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# === TAVILY (Internet Deep Search) ===
TAVILY_API_KEY=tvly-...

# === JOEL AGENT ===
JOEL_DEFAULT_LANGUAGE=pt-BR
JOEL_MAX_SEARCH_RESULTS=10
JOEL_SEARCH_DEPTH=advanced

# === CELERY/REDIS (futuro — processamento async) ===
# CELERY_BROKER_URL=redis://localhost:6379/2
# CELERY_RESULT_BACKEND=redis://localhost:6379/2

# === VPS HOSTINGER (futuro deploy) ===
# VPS_HOST=31.97.171.87
# VPS_USER=root
# VPS_PASSWORD=change-me
# VPS_PROJECT_PATH=/opt/interpreter
# VPS_DOMAIN=interpreter.aresdev.cloud

# === GITHUB ===
GITHUB_REPO=interpreter
GITHUB_CLONE_URL=https://github.com/charliba/interpreter.git
GITHUB_BRANCH=main
# GITHUB_TOKEN=ghp_...
